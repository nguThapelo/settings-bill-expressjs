<div class="row">

    <h1>Actions</h1>

    <table>
        <tr>
            <th>Type</th>
            <th>cost</th>
            <th>when</th>
        </tr>
        {{#each actions}}
        <tr>
            <td>{{type}}</td>
            <td>{{cost}}</td>
            <td>{{timestamp}}</td>
        </tr>
        {{/each}}
    </table>
</div>